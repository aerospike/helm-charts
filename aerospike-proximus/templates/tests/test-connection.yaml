apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aerospike-proximus.fullname" . }}-test-connection"
  labels:
    {{- include "aerospike-proximus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aerospike-proximus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
