apiVersion: v1
kind: Service
metadata:
  name: aerospike-proximus-ingress # Name of your service
  namespace: aerospike
spec:
  selector:
    pp-name: aerospike-proximus-cluster
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer
