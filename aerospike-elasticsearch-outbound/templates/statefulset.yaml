apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "aerospike-elastic-outbound.fullname" . }}
  labels:
    app: {{ include "aerospike-elastic-outbound.fullname" . }}
    {{- include "aerospike-elastic-outbound.labels" . | nindent 4 }}

spec:
  updateStrategy:
    type: RollingUpdate
  podManagementPolicy: Parallel
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  serviceName: {{ include "aerospike-elastic-outbound.fullname" . }}
  selector:
    matchLabels:
      {{- include "aerospike-elastic-outbound.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/configmap: {{ include "aerospike-elastic-outbound.connectorConfig" . | sha256sum }}
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        app: {{ include "aerospike-elastic-outbound.fullname" . }}
        {{- include "aerospike-elastic-outbound.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "aerospike-elastic-outbound.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      volumes:
        {{- if .Values.initContainers }}
        - name: aerospike-elastic-outbound-init
          emptyDir: {}
        {{- end }}
        - name: aerospike-elastic-outbound-configmap
          configMap:
            name: {{ include "aerospike-elastic-outbound.fullname" . }}
            optional: false
        {{- if and .Values.connectorSecrets (not (kindIs "slice" .Values.connectorSecrets)) }}
        {{- fail "secrets should be a list of strings"}}
        {{- end}}
        {{- if and .Values.connectorSecrets (kindIs "slice" .Values.connectorSecrets) }}
        {{- range $secret := .Values.connectorSecrets }}
        - name: {{ $secret }}
          secret:
            secretName: {{ $secret }}
            optional: false
        {{- end}}
        {{- end}}
      {{ if .Values.initContainers }}
      initContainers:
      {{- toYaml .Values.initContainers | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ include "aerospike-elastic-outbound.servicePort" . }}
              protocol: TCP
            - name: manage
              containerPort: {{ (((.Values.connectorConfig).service).manage).port | default 8902 }}
              protocol: TCP
          volumeMounts:
            - name: aerospike-elastic-outbound-configmap
              mountPath: /etc/aerospike-elasticsearch-outbound
            {{- if and .Values.connectorSecrets (not (kindIs "slice" .Values.connectorSecrets)) }}
            {{- fail "secrets should be a list of strings" }}
            {{- end }}
            {{- if and .Values.connectorSecrets (kindIs "slice" .Values.connectorSecrets) }}
            {{- range $secret := .Values.connectorSecrets }}
            - name: {{ $secret }}
              mountPath: /etc/aerospike-elasticsearch-outbound/secrets/{{ $secret }}
            {{- end }}
            {{- end }}
            {{- if .Values.initContainers }}
            - name: aerospike-elastic-outbound-init
              mountPath: /opt/aerospike-elasticsearch-outbound/usr-lib/aerospike-elasticsearch-outbound-custom-transformers.jar
              subPath: aerospike-elastic-outbound-custom-transformers.jar
            {{- end }}
          livenessProbe:
            tcpSocket:
              port: {{ include "aerospike-elastic-outbound.servicePort" . }}
            initialDelaySeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            tcpSocket:
              port: {{ include "aerospike-elastic-outbound.servicePort" . }}
            initialDelaySeconds: 10
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}