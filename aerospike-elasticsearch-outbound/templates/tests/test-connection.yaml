apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aerospike-elastic-outbound.fullname" . }}-test-connection"
  labels:
    {{- include "aerospike-elastic-outbound.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-connection
      image: busybox
      command: ['sh']
      args:
      - -c
      - |
        echo "Testing connection to {{ include "aerospike-elastic-outbound.fullname" . }}"
        nc -z {{ include "aerospike-elastic-outbound.fullname" . }} {{ include "aerospike-elastic-outbound.servicePort" . }}
        if [ $? -eq 0 ]; then
          echo "Connection successful"
        else
          echo "Connection failed"
          exit 1
        fi
  restartPolicy: Never